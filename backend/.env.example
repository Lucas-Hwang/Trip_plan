# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=trip_user
DB_PASSWORD=trip_pass
DB_NAME=trip_db

# JWT
JWT_SECRET=thailand-trip-jwt-secret-key-change-me-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=thailand-trip-refresh-secret-key-change-me
JWT_REFRESH_EXPIRES_IN=7d

# FCM (optional for Phase 5)
# GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json
